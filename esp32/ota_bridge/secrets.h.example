#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to secrets.h and fill in your own values.
// secrets.h is gitignored; this template is tracked.

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASS "your-wifi-password"

// Base URL of the Raspberry Pi firmware server (no trailing slash).
// Use the Pi's LAN address and the port app.py listens on (5000 by default).
#define PI_HOST "http://192.168.1.100:5000"

#endif // SECRETS_H
